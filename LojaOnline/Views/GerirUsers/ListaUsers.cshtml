@model IEnumerable<LojaOnline.Models.ApplicationUser>


@{ 
    ViewBag.Title = "Lista de Utilizadores";
}


<h2>@ViewBag.Title</h2>
  
    @if (TempData["Sucesso"] != null)
    {
        <div class="alert alert-success alert-dismissable fade in">
            <a href="#" class="close" data-dismiss="alert" id="Mensagem" aria-label="close">&times;</a>
            @TempData["Sucesso"]
        </div>
    }

@*<p>
    <button type="button" class="btn btn-primary btn-sm" onclick="location.href='@Url.Action("Register", "Account")'">Novo Utilizador</button>
</p>

@using (Html.BeginForm("ListaUsers", "GerirUsersController", FormMethod.Get))
{
    <p>
        Pesquisar por nome próprio: @Html.TextBox("SearchString", ViewBag.CurrentFilter as string)
        <input type="submit" value="Pesquisar" class="btn btn-default btn-primary" />
        @Html.ActionLink("Limpar", "Index", new { SearchString = "" }, new { @class = "btn btn-default" })
    </p>
}*@

<div class="row">
    <div class="col-md-10">
        <div class="panel-heading">
        </div>
        <div class="panel-body">
            <table class="table table-striped table-bordered table-hover text-center">
                <thead>
                    <tr>
                        <th>
                           Nome Próprio
                        </th>
                        <th>
                           Apelido
                        </th>
                        <th>
                            NIF
                        </th>
                        <th>
                            Morada
                        </th>
                        <th>
                            Código Postal
                        </th>
                    </tr>
                </thead>

                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.NomeProprio)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Apelido)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.NIF)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Morada)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.CodPostal)
                            </td>
                            <td>
                                @Html.ActionLink("Editar", "Editar", new { id = item.Id})                              
                            </td>
                        </tr>
                    }
            </table>
            <br />
            @*Página @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) de @Model.PageCount

            @Html.PagedListPager(Model, page => Url.Action("Index",
    new { page, sortOrder = ViewBag.CurrentSort, currentFilter = ViewBag.CurrentFilter }))*@
        </div>
    </div>
</div>
