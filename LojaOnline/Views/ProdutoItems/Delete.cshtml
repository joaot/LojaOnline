@model LojaOnline.Models.ProdutoItem



<h3>Tem a certeza que pretende apagar o seguinte produto ?</h3>
<div>
    <hr />
    <div class="row">
        <div class="col-md-10">
            <div class="panel-heading">
            </div>
            <div class="panel-body">
                <table class="table table-striped table-bordered table-hover text-center">
                    <thead>
                        <tr>
                            
                            <th>
                                @Html.DisplayNameFor(model => model.numeroSerie)
                            </th>
                            
                        </tr>
                    </thead>
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => Model.numeroSerie)
                            </td>
                        </tr>
                </table>
            </div>
        </div>
  </div>
                    @using (Html.BeginForm())
                    {
                        @Html.AntiForgeryToken()

                        <div class="form-actions no-color">
                            <input type="submit" class="btn btn-primary" value="Apagar" /> |
                            @Html.ActionLink("Voltar atrás", "Index")
                        </div>
                    }
            </div>
